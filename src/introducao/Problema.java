package introducao;

public class Problema {

	/*
	 * Suponha uma classe Produto com os atributos nome e preco.
	 * 
	 * Podemos implementar a comparação de produtos por meio da
	 * implementação da interface Comparable<Produto>
	 * 
	 * Entretanto, desta forma nossa classe não fica fechada
	 * para alteração: se o criterio de comparação mudar,
	 * precisamos alterar a classe Produto.
	 * 
	 * Podemos então usar o default method "sort" da interface
	 * List.
	 * default void sort(Comparator<? super E> c)
	 */
}
